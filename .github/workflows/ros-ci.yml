name: ROS 2 CI Action

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    container:
      image: rostooling/setup-ros-docker:ubuntu-noble-latest
    steps:
      - name: Build and run tests
        uses: ros-tooling/action-ros-ci@v0.4
        with:
          package-name: nav_arbitration
          target-ros2-distro: jazzy
          vcs-repo-file-url: https://github.com/Sustainable-Robotics-Base-for-Crops/nav_interfaces.git
          vcs-repo-file-url: https://github.com/Sustainable-Robotics-Base-for-Crops/nav_lifecycle_manager.git
          import-token: ${{ secrets.GITHUB_TOKEN }}
